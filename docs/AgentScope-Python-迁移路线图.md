# GoGo Agent 后端迁移到 AgentScope Python：长期路线图

> 状态：规划稿，2026-09-23。本文安排后续实现顺序；当前 Python 工作区尚未实现迁移功能。
>
> 来源项目：`/Users/shenmingjie/workSpace/LLMentor-teach/gogo-agent`（Java）；目标项目：`/Volumes/PortableSSD/workspace/gogo-agent`（Python）。
>
> 顺序依据：[《目录.md》](/Volumes/PortableSSD/docs/tengjiaozhai.github.io/目录.md) 的 001–102 篇，下面保持编号递增。文章是学习索引，不能仅凭标题认定 Java 代码已经实现对应能力。
>
> 逐项学习与动手步骤见 [《从零到一学习实施计划》](学习路线/README.md)。

## 目标与边界

将现有 Java 后端的**实际行为**逐步转写到当前 Python 工作区，智能体、工具、记忆和编排以 AgentScope Python 为基础；业务规则、认证、持久化、外部服务接入及 HTTP/SSE 接口仍需由项目代码实现。前端无需重写；等 Python 后端具备兼容接口后，直接复制现有 `frontend/` 并做集成验证。迁移期间保留 Java 项目作为对照基线，不直接改动它的业务数据。

目标是当前已实现功能的行为等价，不把 Java README 的“已完成”或文章标题直接当成运行时事实。尤其是 `ReimbursementAgent`、A2A、沙箱、完整可观测性等，先核对真实实现，再标为“迁移”“补齐”或“仅记录决策”；不把尚未实现的功能混入等价迁移验收。

**排序规则**：按 001–102 前进；同一编号完成后才进入下一编号。论证型文章产出短决策记录、契约或验证结果，不强行新增类。为让早期功能安全可测，启动、身份绑定、测试数据库、模拟外部写操作等最小前置条件可在所涉章节建立；相应的后期章节再做完整能力与加固。第 037/038 节的预订与审批写入须在服务端确认授权，第 097 节再补结构化交互流程。

## 当前基线与技术选择

- Java 源项目使用 Spring Boot、AgentScope Java、MyBatis-Plus、MySQL、Redis、Sa-Token、WebFlux。当前请求主链是 `ChatController → ChatAgentExecutor → AgentPipelineService → 快速意图识别/条件改写 → MasterAgent → 子 Agent`；同一会话的继续消息还可能直接续跑活跃 Agent。`AgentPipelineService.dispatchByIntent` 中的“高置信单意图直跳”代码目前被注释，不能按 README 将其视为现行路径。
- README 声称有 9 个 ReActAgent，实际 `ReActAgent.builder` 只在 Master、行程管理、规划、审核、预订、信息 Agent 中出现；问题改写和意图识别是 `AgentBase` 的单次模型调用，报销 Agent 的 `build()` 仍返回 `null`。迁移清单必须按源码逐一确认。
- 当前 Python 工作区只有 IDE 示例 `main.py` 和无依赖的 `pyproject.toml`，其中声明 `requires-python = ">=3.14"`；现有 `.venv` 是 Python 3.14.0，尚未安装 AgentScope。第 001 步要对选定 AgentScope Python 版本、Python 运行时和数据库驱动做实际安装与最小调用验证，再锁定版本；不要先假定 3.14 兼容。
- 以 **AgentScope Python 2.x** 为学习与迁移目标。官方说明 2.x 与 1.x 有破坏性差异；禁止把旧版 `doc.agentscope.io/tutorial/` 中的 `ReActAgent(..., formatter=...)`、`register_tool_function` 示例直接照搬。当前 2.x 的核心概念是 `Agent`、`Toolkit`、`FunctionTool`、状态与事件；用第 001 步锁定版本的[官方文档](https://docs.agentscope.io/)和实际安装包确认 API。
- 初步选择 **FastAPI 薄接口层 + AgentScope 2.x SDK + 项目自己的业务服务和仓储**，用于保持现有 REST/SSE 契约。第 003 步要与 AgentScope 自带 Agent Service 做一次小型对照；其示例服务不自带最终用户认证，不能仅靠占位 `X-User-ID` 上线。无论选哪条路径，模型生成的 `userId`、审批结论和订单写入都不能绕过服务端鉴权。
- 暂沿用现有业务数据语义和外部系统契约。业务表可在第 003/005 步评估复用；AgentScope Java 的 `MysqlSession`、挂起工具状态与 AgentScope Python 的状态格式不能假定兼容，需明确读取/转换边界。是否迁移历史生产数据、是否切换 AgentScope 自带服务，要在核对契约后单独决策；本计划不预设破坏性数据迁移。

## 每一步的完成定义

每个编号结束时，在同一迁移记录中写明：Java 权威入口与输入/输出、Python 实现或“不编码”的理由、可重复的验证、与基线的差异、下一步依赖。模型和外部服务用固定响应、测试账户或只读调用进行对照；写入动作在隔离数据库及模拟外部服务上验证。阶段验收使用端到端调用链，不只检查文件存在。完成一个阶段后才复制相关调用方与文档，避免 Java/Python 双轨成为最终形态。

### 阶段 A：启动、功能基线、登录与会话（001–005）

| 顺序 | 迁移动作及该步可检查的结果 |
|---|---|
| 001 启动 | 固定 Python/AgentScope 版本和依赖；提供一条可运行的后端启动命令、配置示例、健康检查；记录 Java 启动所需的 MySQL/Redis/模型/外部服务。 |
| 002 功能演示 | 从 Java 现有 API 与测试提取“登录 → 对话 → 差旅单 → 规划 → 审核 → 预订”的行为样例；标注已实现、部分实现、未实现及真实外部副作用。重点记录 `/api/chat/{sessionId}`、`/api/chat/respond` 的挂起/恢复。 |
| 003 架构 | 画出 Python 请求、Agent、业务服务、仓储、外部能力的调用关系；盘点 `/api/auth`、`/api/chat`、`/api/my-travel`、`/api/preferences`、`/api/admin/approvals`、`/api/callback/dingtalk/approval` 的请求/响应契约。 |
| 004 登录 | 迁移账户查询、密码/令牌校验、退出和鉴权错误；所有服务端操作从认证上下文取用户身份。用有效、无效、过期、跨用户请求验证。 |
| 005 会话与消息 | 迁移会话、消息、标题及 Agent 状态的存取边界；核对 Java `MysqlSession` 与 Python 状态格式，不直接混读；验证新建、续聊、重启恢复、用户隔离及顺序一致性。 |

**阶段出口**：Python 服务可独立启动；无前端也能用 HTTP 客户端登录、创建会话、保存/读取消息；有契约样例和隔离测试库。

### 阶段 B：多智能体与意图流水线（006–022）

| 顺序 | 迁移动作及该步可检查的结果 |
|---|---|
| 006 多 Agent 理由 | 写明哪些职责必须分 Agent、哪些应留在业务服务；以实际 Java 调用链证明边界。 |
| 007 Agent 职责 | 列出实际装配的 Agent、轻量 LLM 服务和未完成功能；明确每个 Agent 的输入、输出、工具与状态归属。 |
| 008 意图与改写 | 定义统一的查询、改写结果、意图结果数据结构和代表性样例。 |
| 009 职责位置 | 保留独立的改写/识别步骤；记录为何不放进主 Agent 或 Skill，避免重复决策路径。 |
| 010 非 ReAct 选择 | 核对 Java 真实调用方式；Python 侧为无需工具的步骤选择普通模型调用或对应轻量 Agent，并用结果对照。 |
| 011 多轮改写 | 实现指代消解、上下文补全与无需改写时的短路；用连续多轮样例验收。 |
| 012 三层识别 | 定义规则、向量检索、LLM 的命中阈值、回退与统一结果，保存来源和置信信息。 |
| 013 L0/L1 规则 | 移植明确意图规则、优先级和冲突处理；有效命中、误命中、空输入均测试。 |
| 014 L2 RAG | 移植意图样例库、向量化和相似度判定；固定样例集比较召回与误路由。 |
| 015 多意图识别 | L1/L2 识别多意图时保持顺序、去重和证据；测试组合请求。 |
| 016 多意图执行 | 实现多个意图的调度与结果归并；写操作不因重试或多意图重复执行。 |
| 017 流水线 | 串起快速识别、条件改写、完整识别、路由；对每个分支写可复现用例。 |
| 018 准确率修复 | 迁移 Java 已有规则和阈值修复；比较固定语料的分类/路由差异并记录原因。 |
| 019 直达与主 Agent | 确认当前 Java 的“高置信直跳”已注释；Python 先保持由 MasterAgent 调度，仅将直跳列为独立优化候选及其启用条件。 |
| 020 执行顺序 | 用调用链测试证明“快速识别/条件改写 → 识别 → MasterAgent → 子 Agent”的真实顺序，并覆盖活跃 Agent 直接续跑的分支。 |
| 021 上下文传递 | 定义用户身份、会话 ID、意图、改写、追踪 ID、计划引用的显式上下文契约。 |
| 022 Reactor Context 对应 | 用 Python 的显式参数与必要的 `contextvars` 承接异步上下文；验证并发会话隔离、任务切换和恢复后不串用户。 |

**阶段出口**：真实模型可替换为固定响应测试；给定同一输入，Python 的意图和路由决策与 Java 基线一致，且并发请求不串上下文。

### 阶段 C：主 Agent、模型与身份边界（023–028）

| 顺序 | 迁移动作及该步可检查的结果 |
|---|---|
| 023 MasterAgent | 用 AgentScope Python 建立主协调 Agent，仅开放实际允许调用的子 Agent/工具；验证路由和结果汇总。 |
| 024 关键配置 | 固定提示词、最大轮次、工具并行/超时、异常/中断处理和状态生命周期；记录与 Java 的差异。 |
| 025 Agent 配置对照 | 为每个 Agent 建配置表，确认模型、提示词、工具、记忆、输出格式及权限。 |
| 026 模型分工 | 移植快模型/主模型等实际用途，配置模型服务而非写死密钥；验证请求参数和成本观测。 |
| 027 上次 Agent 快速路由 | 从可信会话状态取活跃 Agent，仅对 Java 当前允许的 continuation 信号直接续跑；跨主题、过期状态回到正常路由。 |
| 028 防 `userId` 幻觉 | 工具的 `userId` 由服务端身份注入和校验，不信任模型参数；验证伪造 ID 无法读写他人数据。 |

**阶段出口**：主 Agent 与至少一个只读子 Agent 的完整对话可运行，身份隔离通过负例测试。

### 阶段 D：差旅管理与基础工具（029–041）

| 顺序 | 迁移动作及该步可检查的结果 |
|---|---|
| 029 行程管理 Agent | 接入差旅单 Agent，固定它与 Master、规划、预订之间的职责。 |
| 030 业务模型 | 逐表核对用户、差旅单、状态、行程及关联关系；保留有效字段、约束和时间/金额语义。 |
| 031 工具规范 | 统一工具名称、输入 schema、输出、错误、权限、副作用及幂等要求；只实现实际需要的工具。 |
| 032 政策工具 | 移植政策精确查询和合规判断；用政策边界值和缺失项验收。 |
| 033 联系信息工具 | 移植授权范围内的用户资料读写；验证字段级授权和缺失信息处理。 |
| 034 政策知识库 | 迁移政策文档读取、切片/索引和引用结果；记录索引重建方式。 |
| 035 差旅单工具 | 迁移创建、查询、修改、提交、取消；事务与状态转移用正反例验证。 |
| 036 冲突检测 | 迁移日期重叠、城市交通衔接和相关政策规则；固定时间边界用例。 |
| 037 预订管理工具 | 先迁移现有预订记录查询/取消的业务契约；写操作在隔离环境验收，禁止重复下单。 |
| 038 审批及管理 | 按现有审批角色、回调与状态机迁移；验证越权、重复审批、过期审批和回调幂等。钉钉回调的来源认证/签名必须先核实并通过负例验证，再允许它改变订单状态。 |
| 039 工具缓存 | 只缓存 Java 已缓存且允许缓存的只读结果，限定键、用户、TTL 和失效条件。 |
| 040 问题推荐 | 以单次模型调用迁移推荐问题服务；失败不能覆盖主回答。 |
| 041 深度思考配置 | 根据实际模型能力和 Java 参数对照，固定是否关闭深度思考；测延迟与回答变化。 |

**阶段出口**：差旅单及审批的核心业务不依赖前端可完成；所有读写均经身份、状态和幂等检查。

### 阶段 E：规划、外部旅行能力和审核（042–060）

| 顺序 | 迁移动作及该步可检查的结果 |
|---|---|
| 042 Plan-and-Execute | 定义规划状态、步骤依赖、失败恢复和用户确认点；以现有 Java 流程为准。 |
| 043 规划 Agent | 接入规划 Agent，验证搜索、候选整理、方案生成与终止条件。 |
| 044 工具而非 Skill | 记录方案计算留在确定性工具的理由，不把核心评分藏入提示词。 |
| 045 方案设计工具 | 逐项移植候选组合、时间/价格/政策/偏好计算；同一数据集比较 Java/Python 输出。 |
| 046 目的地实时查询 | 接入现有实时信息能力；限定超时、来源、过期信息与失败返回。 |
| 047 天气 MCP | 用选定 AgentScope Python 版本的 MCP 接口接入天气服务；验证连接生命周期和错误。 |
| 048 外部能力比较 | 核对 Java 实际启用的 Skill/MCP 与可用凭证，形成“迁移/替代/停用”清单。 |
| 049 途牛 | 对接途牛现有搜索/预订能力；先只读和模拟下单，记录请求/响应契约。 |
| 050 航班管家 | 核对实际使用的 Skill、鉴权和输入输出；以固定响应验证转换。 |
| 051 Rolling Go | 迁移酒店查询与用户隔离；并发用户不共享登录态或工作目录。 |
| 052 审核 Agent 理由 | 明确审核需独立判断的边界与确定性规则边界。 |
| 053 审核 Agent | 接入六维审核；固定结构化输出和拒绝/需修改状态。 |
| 054 最终审核工具 | 移植最终校验，覆盖空方案、超预算、时间冲突和不完整证据。 |
| 055 第三方鉴权 | 统一外部服务凭证归属、授权范围和更新/失效规则；列出每个服务实际策略。 |
| 056 Token 配置 | 实现需 Token 的服务端注入，确保工具和日志不返回密钥。 |
| 057 OAuth PKCE | 仅对源项目实际使用的 OAuth 服务迁移本地回调/换令牌流程；验证 state、code verifier 和过期处理。 |
| 058 外部 Skill 生产化 | 补齐实际需要的超时、退出码、结构化错误、用户隔离和可重复调用。 |
| 059 API Key 管理 | 迁移密钥存取、加密、脱敏、更新及撤销；验证数据库/日志/模型上下文均不泄漏。 |
| 060 规划结果传递 | ReviewAgent 从有所有权校验的计划存储读取 PlanAgent 结果；验证跨会话引用失败。 |

**阶段出口**：隔离环境中从差旅申请到计划审核完整跑通；外部服务可用模拟响应替换，不要求真实下单。

### 阶段 F：预订、可靠性与工具边界（061–067）

| 顺序 | 迁移动作及该步可检查的结果 |
|---|---|
| 061 独立 BookingAgent | 保持预订职责独立，核对其与规划 Agent 的真实装配关系（文档图示存在差异）。 |
| 062 预订 Agent | 实现确认后的下单/取消执行链；隔离环境验证确认凭据、订单去重和失败恢复。 |
| 063 下单/取消边界 | 按真实外部能力选择 Skill 或工具；两者共享同一订单状态与审计记录。 |
| 064 预订数据模型 | 迁移订单、供应商单号、状态、价格和关联差旅单；防止重复回调写错状态。 |
| 065 Skill 与 SubAgent | 为每项能力记录选择依据，避免同一行为在两个路径重复实现。 |
| 066 并行与重试 | 只并行独立只读工具；写入重试必须有幂等键/供应商状态核验。 |
| 067 熔断 | 迁移工具级熔断条件、半开探测和恢复；用连续失败/恢复样例验证。 |

**阶段出口**：模拟供应商下单、取消、回调、超时和重试均不会生成重复订单或越权操作。

### 阶段 G：提示词与上下文工程（068–085）

| 顺序 | 迁移动作及该步可检查的结果 |
|---|---|
| 068 注意力引导 | 迁移实际生效的任务优先级和关键约束；在固定对话集上对照。 |
| 069 工具说明 | 保留必要工具说明，工具 schema 是参数权威来源；测误调用率。 |
| 070 角色人格 | 迁移各 Agent 的身份/语气约束，不混入权限判断。 |
| 071 结构化输出 | 为意图、计划、审核、交互结果定义可验证 schema；无效输出显式失败。 |
| 072 Few Shot | 只保留对固定样例确有改善的示例，记录来源和维护位置。 |
| 073 Step-back | 核对源项目是否实际使用；有则迁移触发条件，无则记录不实施。 |
| 074 Reflexion | 同上，限制重思考轮次和成本，避免无限自循环。 |
| 075 决策/路由表 | 使提示词路由规则与服务端路由不冲突，以服务端判定为准。 |
| 076 动态变量 | 在可信服务端注入时间、用户可见信息和计划引用，验证无法由模型伪造。 |
| 077 拒绝策略 | 迁移职责外请求与高风险操作的拒绝/转交条件，覆盖边界样例。 |
| 078 记忆压缩 | 比较压缩前后关键事实保留率，设置触发阈值和不可压缩内容。 |
| 079 Skill 瘦身 | 裁掉不用的第三方说明，只保留调用所需能力及限制。 |
| 080 工具结果裁剪 | 对大响应去重、截断和引用原始数据；不能裁掉审批/价格/来源等决策字段。 |
| 081 KV 缓存 | 稳定系统提示词前缀并量测命中/成本；无收益则不额外实现。 |
| 082 上下文缓存 | 在实际模型支持时验证缓存隔离与失效；不缓存跨用户敏感内容。 |
| 083 中间文件 | 大候选集存可控中间产物并传引用，验证清理和访问授权。 |
| 084 Skill 正文折叠 | 完成调用后折叠历史长正文，保持后续必要约束。 |
| 085 长期记忆 | 迁移授权范围内的偏好存取、召回及删除；验证用户隔离和事实更新。 |

**阶段出口**：同一回归集的业务判断不退化；上下文大小、延迟和成本有实测记录，优化无效则不保留额外机制。

### 阶段 H：运行保障、隔离与编排（086–094）

| 顺序 | 迁移动作及该步可检查的结果 |
|---|---|
| 086 规划闭环 | 对照“计划 → 执行 → 观察 → 审核 → 修订/结束”状态，验证中断后可恢复到正确步骤。 |
| 087 分层记忆 | 区分当前请求、会话、业务记录、长期偏好；规定保留期和权威来源。 |
| 088 命令护栏 | 对真正需要 shell 的外部 Skill 限定命令、参数、目录和凭证，测试拒绝非法输入。 |
| 089 沙箱 | 核对源系统需求与实际实现；若需运行不可信命令，先完成隔离方案验证再开放。 |
| 090 可观测性 | 建立请求/会话/Agent/工具关联日志和必要指标；敏感数据脱敏后验证故障可定位。 |
| 091 全链路脱敏 | 验证 API、SSE、日志、异常、工具响应、模型上下文的密钥和个人信息处理。 |
| 092 集群打断与恢复 | 迁移中断广播、活跃状态与待处理工具状态；跨进程测试打断、重启、续跑。 |
| 093 编排调度 | 核对是否需要额外调度；若源端只是固定流水线，保留单一权威编排路径。 |
| 094 用户级隔离 | 统一认证上下文与仓储过滤；以跨用户访问、并发会话和外部 Skill 状态验证。 |

**阶段出口**：故障、中断、重启和并发场景可重复验证；记录每种状态由谁持久化、由谁恢复。

### 阶段 I：知识与协议、前端对接、生产部署（095–102）

| 顺序 | 迁移动作及该步可检查的结果 |
|---|---|
| 095 RAG 知识库 | 汇总前面已迁移的政策/景点/签证等知识库，验证更新、索引、引用与权限。 |
| 096 MCP 与 Skill | 汇总外部能力注册、生命周期、错误和凭证隔离；停用无实际调用的旧路径。 |
| 097 HITL | 迁移结构化提问、确认、超时、取消与恢复；写操作必须绑定具体用户和待执行动作。 |
| 098 SSE | 复刻 Java 对话流、进度、工具结果、完成与错误事件的时序及断连处理；覆盖 `message`、`message_id`、`error`、`interrupted`、`user_interaction`、`suggestions` 及实际启用的进度/计划事件，用现有前端 API 契约验收。 |
| 099 A2A | 先核对 Java 是否已有真实 A2A；如仍是待办，只产出独立设计与采用条件，不算等价迁移完成项。 |
| 100 执行链路优化 | 用基线量测快路由、并行只读查询、缓存与模型耗时；只保留证明有效且不改变语义的优化。 |
| 101 Agent 通信选择 | 记录进程内调用、共享存储、事件/协议各自用途；收敛重复通信机制。 |
| 102 会话与集群部署 | 完成数据库/Redis 配置、迁移脚本、服务启动、健康检查、并发隔离与恢复演练；最后复制 `frontend/` 并做端到端验收。 |

**最终出口**：Python 后端独立运行并通过现有前端主要流程；登录、会话、意图、差旅单、规划、审核、预订、审批、HITL 和 SSE 的已实现能力有可追踪的 Java→Python 对照证据。未实现的 Java 功能列入单独待办，不混入完成声明。

## 建议的实施节奏与验收包

按阶段 A→I 依次交付；每次只推进一个编号或一个紧密耦合的编号组。每次提交应包含对应 Python 代码、必要的迁移/配置说明，以及能证明契约与权限边界的最小测试。阶段出口应能独立演示，不能靠尚未迁移的 Java 运行时支撑 Python 主流程。外部供应商不可用时，用契约样本标记“模拟验证”；真实集成验证另记结果。

首轮实施只做 **001–005**。开始前冻结一份 Java API、schema、提示词、外部配置项清单；验证 AgentScope 选定版本能在本机 Python 环境安装并完成最小 Agent/Tool 调用。随后建立 Python 启动与健康检查、登录和会话消息持久化。完成该阶段后，再进入 006–022 的意图和路由。

## 待核对的关键事实

1. `pyproject.toml` 当前要求 Python 3.14；AgentScope Python 与全部数据库/模型依赖的真实兼容范围要以安装和 smoke test 确认。
2. Java README 与 `开发进展.md` 对 BookingAgent 位置、工具/Skill 清单等表述不完全一致；README 还把未生效的单意图直跳写成现行策略，并高估 ReActAgent 数量。迁移时以运行时装配和测试为准。
3. Java README 将报销 Agent 和 A2A 列为未完成/待办。后续若要把它们变成 Python 新功能，需要另定验收标准，不作为 Java 功能等价迁移的阻塞项。
4. 当前尚未核实生产数据迁移与现有前端对 Python API 的逐字段兼容；第 002/003 步建立契约样本，第 098/102 步完成联调验收。
5. `ApprovalCallbackController` 的源码标注“待完善”，在所见控制器代码中未找到登录或签名校验；外围代理是否校验尚未核实。第 038 步先确认来源验证机制，不将该入口按原样开放。

## 关键源码定位

以下路径均相对于 Java 来源项目；这是规划依据，实施时仍需检查当时版本的真实调用链。

| 关注点 | 入口或状态 |
|---|---|
| 聊天与续跑 | `src/main/java/com/gogo/travel/controller/ChatController.java:54`、`:86`、`:117`；`src/main/java/com/gogo/travel/agent/service/ChatAgentExecutor.java:193` |
| 意图流水线与已注释直跳 | `src/main/java/com/gogo/travel/agent/service/AgentPipelineService.java:134`、`:163`、`:206`、`:220` |
| Agent 装配 | `src/main/java/com/gogo/travel/agent/MasterAgent.java:83`；`src/main/java/com/gogo/travel/agent/QueryRewritingAgent.java:30`；`src/main/java/com/gogo/travel/agent/IntentRecognitionAgent.java:47` |
| 数据结构与会话 | `src/main/resources/db/schema.sql:4`；`src/main/java/com/gogo/travel/agent/hook/SessionPersistenceHook.java:73`；`src/main/java/com/gogo/travel/agent/session/PendingToolSessionStore.java:20` |
| SSE 和前端接口 | `src/main/java/com/gogo/travel/agent/service/ChatSseNotifier.java:40`；`frontend/src/api/chat.ts`、`frontend/src/api/auth.ts` |
| 尚未完成 | `src/main/java/com/gogo/travel/agent/ReimbursementAgent.java:19`；`src/main/java/com/gogo/travel/controller/ApprovalCallbackController.java:13` |
